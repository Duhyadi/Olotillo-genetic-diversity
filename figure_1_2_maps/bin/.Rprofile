# ─────────────────────────────────────────────────────────────
# Configuración de entorno para rutas reproducibles en R
# Proyecto: map_&_environmental_conditions
# Autor: Duhyadi
# ─────────────────────────────────────────────────────────────

# Cargar automáticamente el paquete here al iniciar el proyecto
if (requireNamespace("here", quietly = TRUE)) {
  message("📂 Proyecto raíz detectado en: ", here::here())
  options(here.verbose = TRUE)
} else {
  message("⚠️ Paquete 'here' no instalado. Ejecuta install.packages('here')")
}
